type: collection

id:
  - method: default
    inputs:
      defaults:
        collection_id: cmip6

extraction_methods:
  - method: json_file
    inputs:
      dirpath: /home/users/rhys_r_e/stac-eo/output/item/object_store
      terms:
        - mip_era
        - activity_id
        - institution_id
        - source_id
        - table_id
        - var_id
        - version
