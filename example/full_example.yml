paths:
  - /badc/faam/data

type: item

id:
  - method: default
    inputs:
      defaults:
        item_id: item_id

extraction_methods:
  - method: default
    inputs:
      defaults:
        latest: True