paths:
  - /badc/faam/data
categories:
  - label: metadata
    regex: 00README
collections:
  id: undefined
  defaults: {}
  mappings: {}
  overrides: {}
  extraction_methods:
facets:
  defaults:
    properties:
      source_classifier: aircraft
  mappings:
    facet1:
      term1: term2
      term3: term4
  overrides:
    facet2: term1
  extraction_methods:
    name: regex
    inputs:
      regex: (?P<project>\w*)
  common_facets:
    - project
  collection_facets:
    - source_classifier
  item_facets:
    - file_directory
  asset_facets:
    - start_datetime
    - end_datetime
assets:
    defaults: {}
    mappings: {}
    overrides: {}
    extraction_methods: []

